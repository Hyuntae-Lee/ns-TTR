# ns-TTR Void Detection Simulator — 구현 스펙 (Claude Code 인계용)

## 1. 프로젝트 목표

구리 마이크로 실린더(직경 ~80μm, 길이 ~500μm, fused silica 유리에 매립)에 레이저 펌프-프로브 thermoreflectance로 내부 void(빈 공간)를 검출할 수 있는지, 시뮬레이션으로 사전 검증하는 도구. 다른 사람들과 공유할 예정이므로 설치/배포가 쉬운 형태가 필요.

**핵심 요구사항**: 단순히 계산이 되는 것이 아니라, **결과를 검증할 수 있는 도구**여야 함 (에너지 보존, grid convergence, 해석해 비교, 수치 파라미터 민감도 테스트를 GUI에서 바로 실행 가능해야 함).

---

## 2. 물리 모델

### 2.1 지배방정식

1D/2D 축대칭 열확산방정식 (내부 열원 없음 — 레이저는 표면 경계조건으로 처리):

```
ρ c_p ∂T/∂t = ∇·(k∇T)
```

축대칭(r, z) 좌표계, 향후 off-axis void 대응을 위해 3D Cartesian 확장 가능하도록 설계.

### 2.2 표면 경계조건 (레이저 입사면, z=0)

Beer-Lambert 흡수 부피 열원 `q̇(x,t) = I₀(1-R)α_abs·e^(-α_abs·x)·f(t)`을, 흡수깊이(~10-20nm)가 격자 간격보다 훨씬 작다는 조건(δ ≫ 1/α_abs) 하에서 표면 flux로 재표현:

```
-k ∂T/∂x |_{x=0} = I₀(1-R)f(t)
```

- `f(t)`: 시간 프로파일 (square 또는 Gaussian 펄스, 선택 가능해야 함)
- 이 근사는 격자 간격이 광학 흡수깊이의 최소 ~7배 이상일 때 유효 (0.14μm 격자까지는 안전 확인됨)

### 2.3 기타 경계조건

- 측면(r=40μm, 구리-실리카 접촉): flux 연속성 + 온도 연속성 (Kapitza resistance는 추후 옵션)
- 후면(z=500μm): 단열 근사 (열 침투 깊이가 로드 길이보다 충분히 작을 때만 유효 — 관측 시간창이 400μm 침투 깊이에 해당하는 시간을 넘어가면 경고 표시)
- 초기조건: T(x,0) = T_room

### 2.4 재질 물성치

| 재질 | k (W/m·K) | ρ (kg/m³) | c (J/kg·K) | α (m²/s) |
|---|---|---|---|---|
| 구리 | 398 | 8960 | 385 | 1.15×10⁻⁴ |
| Fused silica | 1.38 | 2200 | 740 | 8.5×10⁻⁷ |

구리 열확산계수가 fused silica의 ~130배 — 이 비율이 결과 검증용 물리적 직관 앵커로 사용됨.

---

## 3. Void 깊이별 펄스폭·격자 프리셋 (★ 핵심 UX 요구사항)

### 배경 근거

검출하려는 void 깊이 `d`에 대해 특징 시간 `τ_void ~ d²/α`가 정해지고, 펄스폭은 이보다 충분히 짧아야 신호가 가열 자체에 묻히지 않음. 열 침투 길이 `L_diff = √(α·t_pulse)`를 기준으로 펄스폭이 정해지면, 격자 간격은 그 침투 프로파일을 최소 10등분 해상하도록 정해짐. **펄스폭을 길게 잡으면 얕은 void의 신호가 가열 구간 안에 묻혀 사라지므로, 가장 긴 펄스 하나로 전체 깊이 범위를 커버할 수 없음.** 따라서 목표 void 깊이 범위에 따라 펄스폭+격자를 짝으로 바꿔가며 스윕하는 것이 필수.

### 프리셋 테이블 (구리 기준, D_th=1.16×10⁻⁴ m²/s)

이 표를 **하나의 콤보박스(select)**로 노출한다. 각 항목을 선택하면 펄스폭과 격자 간격(Δz)이 동시에 세팅되고, Δt는 Fourier 안정조건(Fo=D_th·Δt/Δz²≤0.5, 또는 implicit이면 정확도 기준)으로 자동 계산됨. **사용자가 펄스폭과 격자를 숫자로 직접 입력하지 않도록 한다** — 둘을 따로 조작하면 물리적으로 안 맞는 조합이 되기 쉬움.

| 콤보박스 표시 라벨 | 목표 침투 깊이 범위 | 대표 깊이 | 펄스폭 τ_p | 격자 Δz | 비고 |
|---|---|---|---|---|---|
| 표면 극근접 (~2μm) | ~2 μm | 1 μm | 17 ns | 0.14 μm | |
| 얕은 void (2~5μm) | 2~5 μm | 2 μm | 69 ns | 0.28 μm | |
| 서브표면 (5~10μm) | 5~10 μm | 5 μm | 431 ns | 0.71 μm | |
| 중간 깊이 (10~20μm) | 10~20 μm | 10 μm | 1.7 μs | 1.4 μm | |
| 중간 깊이 (20~40μm) | 20~40 μm | 20 μm | 6.9 μs | 2.8 μm | 반경 한계(40μm) 부근 |
| 깊은 편 (40~100μm) | 40~100 μm | 40 μm | 27.6 μs | 5.7 μm | |
| 깊음 (100~200μm) | 100~200 μm | 100 μm | 172 μs | 14.1 μm | 기존 기본값과 유사 |
| 매우 깊음 (200~400μm) | 200~400 μm | 200 μm | 690 μs | 28.3 μm | |
| 최대 깊이 (400~500μm) | 400~500 μm | 400 μm | 2.76 ms | 56.6 μm | ⚠ 후면 단열 경계조건에 근접, 신뢰도 낮음 — UI에 경고 표시 |

- 계산식: `τ_p = 2 × d²/D_th` (2배 안전 여유), `Δz = √(D_th·τ_p)/10`
- 반경 방향(최대 40μm) 커버리지는 축 방향 프리셋에 자동으로 포함되므로 별도 반경 스윕은 불필요
- 마지막 행(400~500μm)은 후면 경계조건 근접으로 인해 UI 상에 주의 문구 표시 필요

---

## 4. Void 모델링 & k_void 선택 (★ 핵심 UX 요구사항)

### 배경 근거

Void의 진짜 열전도율(진공~0, 공기~0.026 W/m·K)을 그대로 쓰면 수치적으로 나눗셈/특이행렬 문제가 생길 수 있어, 실무적으로 0보다 큰 "floor" 값을 씀. 이 floor 값은 **물리량이 아니라 수치적 타협**이므로, 결과가 이 값에 얼마나 민감한지 반드시 눈으로 확인 가능해야 함.

### UI 요구사항

`k_void`도 **슬라이더가 아니라 선택형(콤보박스 또는 라디오 버튼)**으로 제공. 후보값을 이산적으로 제시해 사용자가 값 사이를 스윕하며 민감도를 확인하게 함:

| 콤보박스 표시 라벨 | k_void (W/m·K) | 설명 |
|---|---|---|
| 실제 공기값 (0.026) | 0.026 | 물리적으로 가장 정확, 수치적으로 불안정할 수 있음 |
| 보수적 최소값 (0.1) | 0.1 | 공기값에 가까우면서 안정성 확보 시도 |
| 수치 안정 표준값 (1) | 1.0 | 일반적으로 안정적으로 수렴 |
| 기존 관습값 (10) | 10.0 | 과거 세션에서 쓰였던 값 — critical review에서 "임의적"으로 지적된 값, 기본 선택지에 포함하되 경고 문구 첨부 |

- 각 옵션 선택 후 계산 결과(void 신호 크기)와 에너지 보존 오차를 함께 표시해, 사용자가 옵션 간 비교로 민감도를 직접 확인할 수 있게 함
- "10 W/m·K" 옵션 선택 시 UI에 "이 값은 실제 공기 열전도율보다 약 400배 큽니다. 결과를 실제값(0.026)과 비교해보세요" 같은 안내 문구 표시

---

## 5. 기술 스택

- **Python + Streamlit** (GUI, 설정 → 적용 버튼 → 프로그레스 바 → 재계산 패턴을 프레임워크가 기본 지원)
- **numpy**: 격자 기반 배열 연산
- **scipy.sparse / scipy.sparse.linalg**: implicit(Crank-Nicolson) 시간적분을 위한 연립방정식 풀이
- **Plotly**: 3D/2D 온도장 시각화 (인터랙션 품질은 우선순위 낮음, 정확성이 우선)
- 계산 방식: **Implicit (Crank-Nicolson)** — explicit의 안정성 제약(Δt ∝ Δz²)이 짧은 펄스+미세 격자 조합에서 감당 불가능한 스텝 수를 요구하므로

---

## 6. UX 흐름

```
[설정 패널]
  - Void 깊이 프리셋 (콤보박스, §3 표) → 펄스폭+격자 동시 결정
  - k_void 프리셋 (콤보박스, §4 표)
  - 기타: void 위치(축/반경), void 크기, 펄스 프로파일(square/Gaussian)
        ↓ '적용' 버튼 클릭
[재계산 트리거]
  - 프로그레스 바 표시 (Streamlit st.progress)
  - 계산 스텝마다 진행률 갱신
        ↓ 완료
[결과 패널]
  - 온도장 시각화 (Plotly)
  - 검증 지표 자동 표시: 에너지 보존 오차(%), 사용된 Δz/Δt/τ_p 값
  - (선택) 같은 void 깊이에서 k_void 옵션별 결과 나란히 비교
```

---

## 7. 검증 기능 (반드시 GUI에 노출)

- **에너지 보존 체크**: 매 계산마다 입력 에너지 vs 저장된 열에너지 비교, %오차 표시
- **Grid convergence 테스트 버튼**: 같은 조건에서 격자를 2배 조밀하게 자동 재계산 후 결과 비교
- **해석해 비교 (void 없는 baseline)**: Carslaw & Jaeger semi-infinite solid + Gaussian spot 해석해와 자동 비교 오버레이
- **k_void 민감도 테스트**: §4의 옵션들을 자동으로 순회하며 결과 비교 (배치 실행)

---

## 8. 배포

- Streamlit Community Cloud에 배포해 링크 공유 (설치 파일 불필요)
- 로컬 실행: `streamlit run app.py`

---

## 참고: 프로젝트 배경 물성치/이론 요약

이 스펙 문서 외에 다음 파일들도 프로젝트에 함께 배치할 것:
- `열확산방정식유도.pdf` — 지배방정식 및 표면 경계조건 유도 전체 과정
- `열확산방정식_학습정리_ExplicitImplicit.pdf` — Explicit/Implicit 시간적분 방식 비교
